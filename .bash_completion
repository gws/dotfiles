### This is sourced at the end of the bash-completion startup sequence.

if [ -d "${HOME}/.bash_completion.d" ]; then
  for i in "${HOME}/.bash_completion.d/"*; do
    if [ -r "$i" ]; then
      . "$i"
    fi
  done
  unset i
fi
